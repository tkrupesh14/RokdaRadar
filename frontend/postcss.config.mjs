// Tailwind v4 is here only to support the Aceternity UI components (they are
// authored as Tailwind utility classes). The hand-written design system in
// app/globals.css remains the source of truth for the site's look.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
